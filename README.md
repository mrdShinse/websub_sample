# Requirements

- Docker for mac
- docker-sync

# Booting

```
$ docker-compose Build
$ docker-compose run websub_sample_publisher bash
$ bundle install
$ exit
$ docker-sync start
$ docker-compose up
```
